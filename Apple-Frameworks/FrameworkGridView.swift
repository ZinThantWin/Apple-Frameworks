import SwiftUI

struct FrameworkGridView : View {
    var body : some View {
        VStack{
            Text("Hello Swift!")
                .font(.system(.largeTitle))
        }
    }
}

#Preview {
    FrameworkGridView()
}
